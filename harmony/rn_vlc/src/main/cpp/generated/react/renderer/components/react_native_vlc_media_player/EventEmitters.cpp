
/**
 * This code was generated by [react-native-codegen](https://www.npmjs.com/package/react-native-codegen).
 *
 * Do not edit this file as changes may cause incorrect behavior and will be lost
 * once the code is regenerated.
 *
 * @generated by codegen project: GenerateEventEmitterCpp.js
 */

#include <react/renderer/components/react_native_vlc_media_player/EventEmitters.h>


namespace facebook {
namespace react {

void RNVLCEventEmitter::onPlaying(OnPlaying $event) const {
  dispatchEvent("playing", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
$payload.setProperty(runtime, "duration", $event.duration);
$payload.setProperty(runtime, "seekable", $event.seekable);
    return $payload;
  });
}


void RNVLCEventEmitter::onProgress(OnProgress $event) const {
  dispatchEvent("progress", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
$payload.setProperty(runtime, "duration", $event.duration);
$payload.setProperty(runtime, "currentTime", $event.currentTime);
$payload.setProperty(runtime, "position", $event.position);
$payload.setProperty(runtime, "remainingTime", $event.remainingTime);
    return $payload;
  });
}


void RNVLCEventEmitter::onPaused(OnPaused $event) const {
  dispatchEvent("paused", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
    return $payload;
  });
}


void RNVLCEventEmitter::onStopped(OnStopped $event) const {
  dispatchEvent("stopped", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
    return $payload;
  });
}


void RNVLCEventEmitter::onBuffering(OnBuffering $event) const {
  dispatchEvent("buffering", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
    return $payload;
  });
}


void RNVLCEventEmitter::onEnd(OnEnd $event) const {
  dispatchEvent("end", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
    return $payload;
  });
}


void RNVLCEventEmitter::onError(OnError $event) const {
  dispatchEvent("error", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
    return $payload;
  });
}


void RNVLCEventEmitter::onLoad(OnLoad $event) const {
  dispatchEvent("load", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
$payload.setProperty(runtime, "duration", $event.duration);
{
  auto videoSize = jsi::Object(runtime);
  videoSize.setProperty(runtime, "width", $event.videoSize.width);
  videoSize.setProperty(runtime, "height", $event.videoSize.height);
  $payload.setProperty(runtime, "videoSize", videoSize);
}

    auto audioTracks = jsi::Array(runtime, $event.audioTracks.size());
    size_t audioTracksIndex = 0;
    for (auto audioTracksValue : $event.audioTracks) {
      auto audioTracksObject = jsi::Object(runtime);
      audioTracksObject.setProperty(runtime, "id", audioTracksValue.id);
audioTracksObject.setProperty(runtime, "name", audioTracksValue.name);
      audioTracks.setValueAtIndex(runtime, audioTracksIndex++, audioTracksObject);
    }
    $payload.setProperty(runtime, "audioTracks", audioTracks);
  

    auto textTracksJson = jsi::Array(runtime, $event.textTracksJson.size());
    size_t textTracksJsonIndex = 0;
    for (auto textTracksJsonValue : $event.textTracksJson) {
      auto textTracksJsonObject = jsi::Object(runtime);
      textTracksJsonObject.setProperty(runtime, "id", textTracksJsonValue.id);
textTracksJsonObject.setProperty(runtime, "name", textTracksJsonValue.name);
      textTracksJson.setValueAtIndex(runtime, textTracksJsonIndex++, textTracksJsonObject);
    }
    $payload.setProperty(runtime, "textTracksJson", textTracksJson);
  
    return $payload;
  });
}


void RNVLCEventEmitter::onRecordingCreated(OnRecordingCreated $event) const {
  dispatchEvent("recordingCreated", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "path", $event.path);
    return $payload;
  });
}


void RNVLCEventEmitter::onSnapshot(OnSnapshot $event) const {
  dispatchEvent("snapshot", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "path", $event.path);
$payload.setProperty(runtime, "success", $event.success);
$payload.setProperty(runtime, "error", $event.error);
    return $payload;
  });
}


void RNVLCEventEmitter::onOpen(OnOpen $event) const {
  dispatchEvent("open", [$event=std::move($event)](jsi::Runtime &runtime) {
    auto $payload = jsi::Object(runtime);
    $payload.setProperty(runtime, "target", $event.target);
    return $payload;
  });
}

} // namespace react
} // namespace facebook
